{% extends 'base.html.twig' %}
{% block body %}
    <div class="admin">
        <div >
            <div class="container-fluid">
                <div class="row" style="border-top: thick double white">
                    <div class="col-md-2  bg-dark" style="color:white">
                        <h5 class="mt-3 perso">Gestion Personnel</h5>
                        <ul class="uliste text-center mt-4" >
                            <li><a href="{{ path('admin_add_personnel') }}">Ajouter</a></li>
                            <li><a href="{{ path('app_listUser') }}">Liste personnel</a></li>
                        </ul>
                        <h5 class="mt-3 perso">Gestion Statut</h5>
                        <ul class="uliste text-center mt-4" >
                            <li><a href="{{ path('admin_add_statut') }}">Ajouter</a></li>
                            <li><a href="{{ path('admin_select_statut') }}">Modifier</a></li>
                        </ul>
                    </div>
                    <div class="col-md-10">
                        <div class="container-fluid contenu">
                            {% block content %} {% endblock %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
